/*
  local variables:
  buffer-name: "editor/view"
  end:
*/


App.EditorView = Em.View.extend({
  templateName: 'editor',
  classNames: ['editor'],
});

App.ListsView = Em.View.extend({
  templateName: 'lists'
});
