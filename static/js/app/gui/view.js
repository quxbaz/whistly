/*
  local variables:
  buffer-name: "gui/view"
  end:
*/


App.GuiView = Em.View.extend({
  templateName: 'gui',
  classNames: ['gui']
});

App.ListsView = Em.View.extend({
  templateName: 'lists'
});
